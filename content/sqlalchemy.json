{
  "id": "sqlalchemy",
  "title": "SQLAlchemy Deep Dive",
  "description": "Models, sessions, queries, relationships, loading strategies, migrations, transactions, raw SQL, and N+1 problems.",
  "order": 5,
  "tags": [
    "sqlalchemy",
    "session",
    "orm",
    "n+1"
  ],
  "lessons": [
    {
      "id": "sqlalchemy-models-sessions",
      "title": "Models and Sessions",
      "summary": "Models map classes to tables; sessions track and persist unit-of-work changes.",
      "explanation": "Models map classes to tables; sessions track and persist unit-of-work changes. In interviews, explain the concept, when it applies, and the trade-off it introduces. Use concrete examples from the provided material instead of definitions alone.",
      "key_points": [
        "model",
        "session",
        "unit of work"
      ],
      "examples": [
        "session.add(user); session.commit() persists a new row."
      ],
      "interview_questions": [
        "When would you apply models and sessions?",
        "What failure mode appears when models and sessions is misunderstood?"
      ],
      "difficulty": 1,
      "tags": [
        "model",
        "session",
        "unit of work"
      ]
    },
    {
      "id": "sqlalchemy-queries-relationships",
      "title": "Queries and Relationships",
      "summary": "ORM queries fetch mapped objects; relationships express one-to-many and many-to-many links.",
      "explanation": "ORM queries fetch mapped objects; relationships express one-to-many and many-to-many links. In interviews, explain the concept, when it applies, and the trade-off it introduces. Use concrete examples from the provided material instead of definitions alone.",
      "key_points": [
        "query",
        "relationship",
        "foreign key"
      ],
      "examples": [
        "User.orders can represent a one-to-many relationship."
      ],
      "interview_questions": [
        "When would you apply queries and relationships?",
        "What failure mode appears when queries and relationships is misunderstood?"
      ],
      "difficulty": 1,
      "tags": [
        "query",
        "relationship",
        "foreign key"
      ]
    },
    {
      "id": "sqlalchemy-loading-nplus1",
      "title": "Loading Strategies and N+1",
      "summary": "Lazy loading inside loops can trigger N+1 queries; eager loading fetches related rows earlier.",
      "explanation": "Lazy loading inside loops can trigger N+1 queries; eager loading fetches related rows earlier. In interviews, explain the concept, when it applies, and the trade-off it introduces. Use concrete examples from the provided material instead of definitions alone.",
      "key_points": [
        "lazy loading",
        "selectinload",
        "joinedload",
        "n+1"
      ],
      "examples": [
        "Loading 100 users then accessing orders can execute 101 queries."
      ],
      "interview_questions": [
        "When would you apply loading strategies and n+1?",
        "What failure mode appears when loading strategies and n+1 is misunderstood?"
      ],
      "difficulty": 2,
      "tags": [
        "lazy loading",
        "selectinload",
        "joinedload"
      ]
    },
    {
      "id": "sqlalchemy-migrations-transactions",
      "title": "Migrations, Transactions, and Raw SQL",
      "summary": "Migrations evolve schema; transactions protect consistency; raw SQL remains useful for complex queries.",
      "explanation": "Migrations evolve schema; transactions protect consistency; raw SQL remains useful for complex queries. In interviews, explain the concept, when it applies, and the trade-off it introduces. Use concrete examples from the provided material instead of definitions alone.",
      "key_points": [
        "migration",
        "transaction",
        "raw sql"
      ],
      "examples": [
        "Alembic records schema changes in migration files."
      ],
      "interview_questions": [
        "When would you apply migrations, transactions, and raw sql?",
        "What failure mode appears when migrations, transactions, and raw sql is misunderstood?"
      ],
      "difficulty": 2,
      "tags": [
        "migration",
        "transaction",
        "raw sql"
      ]
    }
  ],
  "questions": [
    {
      "id": "sqlalchemy-q01",
      "type": "matching",
      "prompt": "Match SQLAlchemy core concepts.",
      "explanation": "SQLAlchemy maps Python objects to relational tables and uses sessions to track changes.",
      "difficulty": 1,
      "tags": [
        "model",
        "session",
        "relationship"
      ],
      "pairs": [
        {
          "left": "Model",
          "right": "Python class mapped to a table"
        },
        {
          "left": "Session",
          "right": "Unit-of-work boundary for persistence"
        },
        {
          "left": "Relationship",
          "right": "Object link between tables"
        }
      ],
      "answer": {
        "Model": "Python class mapped to a table",
        "Session": "Unit-of-work boundary for persistence",
        "Relationship": "Object link between tables"
      },
      "concept_panel": {
        "title": "SQLAlchemy Session and Unit of Work",
        "explanation": "A SQLAlchemy Session tracks objects, pending changes, and the transaction boundary for a unit of work. Calling `add` marks an object for persistence; `flush` sends SQL without committing; `commit` completes the transaction. Sessions should usually be scoped per request in web apps so identity maps and transaction state do not leak. This concept is different from browser sessions.",
        "key_takeaways": [
          "Session tracks ORM object state.",
          "flush sends SQL inside the transaction.",
          "Web apps should avoid one global shared Session."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand ORM persistence boundaries and transaction scope.",
        "practical_example": "Create a user, add it to the request-scoped session, flush to get its ID, then commit.",
        "diagram": {
          "type": "flow",
          "title": "Unit of work",
          "nodes": [
            {
              "label": "Create object"
            },
            {
              "label": "Session.add"
            },
            {
              "label": "Flush SQL"
            },
            {
              "label": "Commit"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q02",
      "type": "ordering",
      "prompt": "Order a typical SQLAlchemy create flow.",
      "explanation": "The session tracks pending objects; flush sends SQL; commit completes the transaction.",
      "difficulty": 2,
      "tags": [
        "session",
        "unit of work"
      ],
      "options": [
        "Create model object",
        "Add to session",
        "Flush or commit",
        "Refresh/read generated fields"
      ],
      "answer": [
        "Create model object",
        "Add to session",
        "Flush or commit",
        "Refresh/read generated fields"
      ],
      "concept_panel": {
        "title": "SQLAlchemy Session and Unit of Work",
        "explanation": "A SQLAlchemy Session tracks objects, pending changes, and the transaction boundary for a unit of work. Calling `add` marks an object for persistence; `flush` sends SQL without committing; `commit` completes the transaction. Sessions should usually be scoped per request in web apps so identity maps and transaction state do not leak. This concept is different from browser sessions.",
        "key_takeaways": [
          "Session tracks ORM object state.",
          "flush sends SQL inside the transaction.",
          "Web apps should avoid one global shared Session."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand ORM persistence boundaries and transaction scope.",
        "practical_example": "Create a user, add it to the request-scoped session, flush to get its ID, then commit.",
        "diagram": {
          "type": "flow",
          "title": "Unit of work",
          "nodes": [
            {
              "label": "Create object"
            },
            {
              "label": "Session.add"
            },
            {
              "label": "Flush SQL"
            },
            {
              "label": "Commit"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q03",
      "type": "scenario_choice",
      "prompt": "You need the new row ID before committing. Which operation can send pending SQL?",
      "explanation": "Flush sends pending changes to the database within the transaction, often making generated IDs available before commit.",
      "difficulty": 2,
      "tags": [
        "flush",
        "session"
      ],
      "options": [
        "flush",
        "joinedload",
        "rollback",
        "relationship"
      ],
      "answer": "flush",
      "concept_panel": {
        "title": "SQLAlchemy Session and Unit of Work",
        "explanation": "A SQLAlchemy Session tracks objects, pending changes, and the transaction boundary for a unit of work. Calling `add` marks an object for persistence; `flush` sends SQL without committing; `commit` completes the transaction. Sessions should usually be scoped per request in web apps so identity maps and transaction state do not leak. This concept is different from browser sessions.",
        "key_takeaways": [
          "Session tracks ORM object state.",
          "flush sends SQL inside the transaction.",
          "Web apps should avoid one global shared Session."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand ORM persistence boundaries and transaction scope.",
        "practical_example": "Create a user, add it to the request-scoped session, flush to get its ID, then commit.",
        "diagram": {
          "type": "flow",
          "title": "Unit of work",
          "nodes": [
            {
              "label": "Create object"
            },
            {
              "label": "Session.add"
            },
            {
              "label": "Flush SQL"
            },
            {
              "label": "Commit"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q04",
      "type": "bug_hunt",
      "prompt": "Loading 100 users then reading user.orders causes 101 SQL queries. What is the problem?",
      "explanation": "Lazy relationship access inside a loop can issue one query per parent row, creating the N+1 pattern.",
      "difficulty": 2,
      "tags": [
        "n+1",
        "lazy loading"
      ],
      "options": [
        "N+1 from lazy loading",
        "CORS preflight",
        "Missing Swagger",
        "DNS lookup"
      ],
      "answer": "N+1 from lazy loading",
      "concept_panel": {
        "title": "N+1 Queries",
        "explanation": "N+1 happens when one query loads parent rows and then one additional query runs for each parent as code accesses a lazy relationship. Loading 100 users and reading `user.orders` in a loop can become 101 queries. SQLAlchemy loading options such as `selectinload` and `joinedload` fetch related data earlier. The fix depends on relationship size and query shape.",
        "key_takeaways": [
          "Lazy relationship access inside loops can explode query count.",
          "selectinload is often safer for collections.",
          "joinedload can be useful but may duplicate parent rows."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand ORM performance instead of treating the ORM as invisible.",
        "practical_example": "Use `select(User).options(selectinload(User.orders))` before looping over users and orders.",
        "diagram": {
          "type": "flow",
          "title": "N+1 pattern",
          "nodes": [
            {
              "label": "SELECT users"
            },
            {
              "label": "Loop"
            },
            {
              "label": "SELECT orders"
            },
            {
              "label": "SELECT orders"
            },
            {
              "label": "101 queries",
              "status": "hot"
            },
            {
              "label": "selectinload",
              "status": "good"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q05",
      "type": "multi_select",
      "prompt": "Which loading strategies can fix many N+1 collection reads?",
      "explanation": "selectinload and joinedload eager-load related data instead of lazily querying each row.",
      "difficulty": 2,
      "tags": [
        "selectinload",
        "joinedload",
        "eager"
      ],
      "options": [
        "selectinload",
        "joinedload",
        "time.sleep",
        "os.system"
      ],
      "answer": [
        "selectinload",
        "joinedload"
      ],
      "concept_panel": {
        "title": "SQLAlchemy Loading Strategies",
        "explanation": "SQLAlchemy relationships can load lazily or eagerly. Lazy loading waits until the attribute is accessed, which can create N+1 queries. `selectinload` performs a second SELECT for related rows and is often a good fit for collections. `joinedload` uses a JOIN and can be efficient for smaller relationships but may duplicate parent data in large collection results.",
        "key_takeaways": [
          "Lazy loading can hide database work.",
          "selectinload often fits one-to-many collections.",
          "joinedload trades fewer round trips for larger joined result sets."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you can diagnose SQLAlchemy query behavior in production.",
        "practical_example": "`select(User).options(selectinload(User.orders))` loads users and their orders without one query per user.",
        "diagram": {
          "type": "compare",
          "title": "Eager loading choice",
          "columns": [
            {
              "title": "selectinload",
              "items": [
                "Second SELECT",
                "Good for collections",
                "Avoids huge JOIN rows"
              ]
            },
            {
              "title": "joinedload",
              "items": [
                "Single JOIN",
                "Good for small related data",
                "Can duplicate parent rows"
              ]
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q06",
      "type": "scenario_choice",
      "prompt": "A collection relationship creates huge duplicate parent rows with a JOIN. Which eager strategy is often safer?",
      "explanation": "selectinload loads collections with a second SELECT and avoids one huge joined result for large collections.",
      "difficulty": 2,
      "tags": [
        "selectinload",
        "loading"
      ],
      "options": [
        "selectinload",
        "raw string concat",
        "global session",
        "CORS"
      ],
      "answer": "selectinload",
      "concept_panel": {
        "title": "SQLAlchemy Loading Strategies",
        "explanation": "SQLAlchemy relationships can load lazily or eagerly. Lazy loading waits until the attribute is accessed, which can create N+1 queries. `selectinload` performs a second SELECT for related rows and is often a good fit for collections. `joinedload` uses a JOIN and can be efficient for smaller relationships but may duplicate parent data in large collection results.",
        "key_takeaways": [
          "Lazy loading can hide database work.",
          "selectinload often fits one-to-many collections.",
          "joinedload trades fewer round trips for larger joined result sets."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you can diagnose SQLAlchemy query behavior in production.",
        "practical_example": "`select(User).options(selectinload(User.orders))` loads users and their orders without one query per user.",
        "diagram": {
          "type": "compare",
          "title": "Eager loading choice",
          "columns": [
            {
              "title": "selectinload",
              "items": [
                "Second SELECT",
                "Good for collections",
                "Avoids huge JOIN rows"
              ]
            },
            {
              "title": "joinedload",
              "items": [
                "Single JOIN",
                "Good for small related data",
                "Can duplicate parent rows"
              ]
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q07",
      "type": "matching",
      "prompt": "Match migration terms.",
      "explanation": "Migrations make database schema evolution explicit, reviewable, and repeatable.",
      "difficulty": 2,
      "tags": [
        "migration",
        "alembic"
      ],
      "pairs": [
        {
          "left": "Migration",
          "right": "Versioned schema change"
        },
        {
          "left": "Alembic",
          "right": "Common SQLAlchemy migration tool"
        },
        {
          "left": "Rollback",
          "right": "Reverts a schema change"
        }
      ],
      "answer": {
        "Migration": "Versioned schema change",
        "Alembic": "Common SQLAlchemy migration tool",
        "Rollback": "Reverts a schema change"
      },
      "concept_panel": {
        "title": "Schema Migrations",
        "explanation": "Migrations are versioned database schema changes. In SQLAlchemy projects, Alembic is commonly used to create, review, apply, and roll back those changes. Migrations make schema evolution repeatable across local, test, and production databases. They are operational artifacts, not just generated files.",
        "key_takeaways": [
          "Migrations version schema changes.",
          "Alembic is common with SQLAlchemy.",
          "Rollback plans reduce deployment risk."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand production schema evolution.",
        "practical_example": "Add a nullable column in one migration, deploy readers, then later enforce constraints when writers are ready.",
        "diagram": {
          "type": "flow",
          "title": "Migration lifecycle",
          "nodes": [
            {
              "label": "Model change"
            },
            {
              "label": "Migration file"
            },
            {
              "label": "Review"
            },
            {
              "label": "Apply"
            },
            {
              "label": "Rollback if needed"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q08",
      "type": "scenario_choice",
      "prompt": "An ORM query is too complex and database-specific. What escape hatch can be appropriate?",
      "explanation": "SQLAlchemy allows raw SQL when needed, but parameters are essential to avoid injection risks.",
      "difficulty": 3,
      "tags": [
        "raw sql",
        "query"
      ],
      "options": [
        "Raw SQL with parameters",
        "String interpolation with user input",
        "Disable transactions",
        "Ignore indexes"
      ],
      "answer": "Raw SQL with parameters",
      "concept_panel": {
        "title": "Raw SQL Escape Hatches",
        "explanation": "ORMs cover common queries, but complex reporting, database-specific features, or performance-critical paths may need raw SQL. Raw SQL should still use parameters rather than string interpolation, otherwise user input can become SQL injection. The point is not avoiding ORMs; it is knowing when to step below the abstraction safely.",
        "key_takeaways": [
          "Raw SQL can be appropriate for complex queries.",
          "Always parameterize user input.",
          "Measure before replacing ORM queries."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you can use lower-level database tools safely.",
        "practical_example": "Use a parameterized SQL statement for a database-specific reporting query that the ORM expresses poorly.",
        "diagram": {
          "type": "flow",
          "title": "Safe raw SQL",
          "nodes": [
            {
              "label": "Complex query"
            },
            {
              "label": "Parameterized SQL"
            },
            {
              "label": "Database"
            },
            {
              "label": "Typed result"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q09",
      "type": "ordering",
      "prompt": "Order a safe transactional update.",
      "explanation": "A session often scopes a transaction; failures should roll back pending changes.",
      "difficulty": 2,
      "tags": [
        "transaction",
        "session"
      ],
      "options": [
        "Open session/transaction",
        "Load rows",
        "Apply changes",
        "Commit or rollback"
      ],
      "answer": [
        "Open session/transaction",
        "Load rows",
        "Apply changes",
        "Commit or rollback"
      ],
      "concept_panel": {
        "title": "SQLAlchemy Session and Unit of Work",
        "explanation": "A SQLAlchemy Session tracks objects, pending changes, and the transaction boundary for a unit of work. Calling `add` marks an object for persistence; `flush` sends SQL without committing; `commit` completes the transaction. Sessions should usually be scoped per request in web apps so identity maps and transaction state do not leak. This concept is different from browser sessions.",
        "key_takeaways": [
          "Session tracks ORM object state.",
          "flush sends SQL inside the transaction.",
          "Web apps should avoid one global shared Session."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand ORM persistence boundaries and transaction scope.",
        "practical_example": "Create a user, add it to the request-scoped session, flush to get its ID, then commit.",
        "diagram": {
          "type": "flow",
          "title": "Unit of work",
          "nodes": [
            {
              "label": "Create object"
            },
            {
              "label": "Session.add"
            },
            {
              "label": "Flush SQL"
            },
            {
              "label": "Commit"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q10",
      "type": "code_fill",
      "prompt": "Fill the SQLAlchemy eager-loading option.",
      "explanation": "selectinload is an eager-loading option commonly used to avoid N+1 queries for collections.",
      "difficulty": 2,
      "tags": [
        "selectinload",
        "eager"
      ],
      "options": [
        "selectinload",
        "Depends",
        "pytest",
        "BaseModel"
      ],
      "code": "stmt = select(User).options(____(User.orders))",
      "answer": "selectinload",
      "concept_panel": {
        "title": "SQLAlchemy Loading Strategies",
        "explanation": "SQLAlchemy relationships can load lazily or eagerly. Lazy loading waits until the attribute is accessed, which can create N+1 queries. `selectinload` performs a second SELECT for related rows and is often a good fit for collections. `joinedload` uses a JOIN and can be efficient for smaller relationships but may duplicate parent data in large collection results.",
        "key_takeaways": [
          "Lazy loading can hide database work.",
          "selectinload often fits one-to-many collections.",
          "joinedload trades fewer round trips for larger joined result sets."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you can diagnose SQLAlchemy query behavior in production.",
        "practical_example": "`select(User).options(selectinload(User.orders))` loads users and their orders without one query per user.",
        "diagram": {
          "type": "compare",
          "title": "Eager loading choice",
          "columns": [
            {
              "title": "selectinload",
              "items": [
                "Second SELECT",
                "Good for collections",
                "Avoids huge JOIN rows"
              ]
            },
            {
              "title": "joinedload",
              "items": [
                "Single JOIN",
                "Good for small related data",
                "Can duplicate parent rows"
              ]
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q11",
      "type": "bug_hunt",
      "prompt": "A web app shares one global SQLAlchemy Session across all requests. What is the risk?",
      "explanation": "Sessions should be scoped carefully, often per request, so transactions and identity maps do not leak across users.",
      "difficulty": 3,
      "tags": [
        "session",
        "unit of work"
      ],
      "options": [
        "Request state leaks and transaction boundaries blur",
        "DNS becomes slower",
        "Swagger disappears",
        "PATCH becomes GET"
      ],
      "answer": "Request state leaks and transaction boundaries blur",
      "concept_panel": {
        "title": "SQLAlchemy Session and Unit of Work",
        "explanation": "A SQLAlchemy Session tracks objects, pending changes, and the transaction boundary for a unit of work. Calling `add` marks an object for persistence; `flush` sends SQL without committing; `commit` completes the transaction. Sessions should usually be scoped per request in web apps so identity maps and transaction state do not leak. This concept is different from browser sessions.",
        "key_takeaways": [
          "Session tracks ORM object state.",
          "flush sends SQL inside the transaction.",
          "Web apps should avoid one global shared Session."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand ORM persistence boundaries and transaction scope.",
        "practical_example": "Create a user, add it to the request-scoped session, flush to get its ID, then commit.",
        "diagram": {
          "type": "flow",
          "title": "Unit of work",
          "nodes": [
            {
              "label": "Create object"
            },
            {
              "label": "Session.add"
            },
            {
              "label": "Flush SQL"
            },
            {
              "label": "Commit"
            }
          ]
        }
      }
    },
    {
      "id": "sqlalchemy-q12",
      "type": "matching",
      "prompt": "Match relationship shapes.",
      "explanation": "SQLAlchemy relationships mirror relational foreign-key structures and association tables.",
      "difficulty": 1,
      "tags": [
        "relationship",
        "foreign key"
      ],
      "pairs": [
        {
          "left": "One-to-many",
          "right": "User has many orders"
        },
        {
          "left": "Many-to-many",
          "right": "Students enroll in courses"
        },
        {
          "left": "Foreign key",
          "right": "Column that points to another table"
        }
      ],
      "answer": {
        "One-to-many": "User has many orders",
        "Many-to-many": "Students enroll in courses",
        "Foreign key": "Column that points to another table"
      },
      "concept_panel": {
        "title": "Keys, Relationships, and JOINs",
        "explanation": "Primary keys uniquely identify rows, and foreign keys connect rows across tables. JOINs combine related rows so normalized data can answer richer questions. Many-to-many relationships usually need a junction table. This is foundational because database schema design determines both correctness and query performance.",
        "key_takeaways": [
          "Primary keys identify rows.",
          "Foreign keys model relationships.",
          "JOINs reconstruct related data at query time."
        ],
        "interview_insight": "Interviewers usually ask this concept to evaluate whether you understand relational modeling, not just SQL syntax.",
        "practical_example": "orders.user_id references users.id, letting a query join orders to the owning user.",
        "diagram": {
          "type": "network",
          "title": "Relational link",
          "nodes": [
            {
              "id": "users",
              "label": "users"
            },
            {
              "id": "orders",
              "label": "orders"
            },
            {
              "id": "order_items",
              "label": "order_items"
            }
          ],
          "edges": [
            {
              "from": "orders",
              "to": "users",
              "label": "foreign key"
            },
            {
              "from": "order_items",
              "to": "orders",
              "label": "foreign key"
            }
          ]
        }
      }
    }
  ],
  "coding_challenges": [
    {
      "id": "sqlalchemy-classify-status",
      "title": "SQLAlchemy Deep Dive status classifier",
      "instructions": "Implement classify_status_code(code: int) -> str. Return success for 200-299, redirect for 300-399, client_error for 400-499, server_error for 500-599, and unknown otherwise.",
      "starter_code": "def classify_status_code(code: int) -> str:\n    pass\n",
      "function_signature": "def classify_status_code(code: int) -> str",
      "visible_tests": [
        {
          "name": "created",
          "call": "classify_status_code(201)",
          "expected": "success"
        },
        {
          "name": "not found",
          "call": "classify_status_code(404)",
          "expected": "client_error"
        }
      ],
      "hidden_tests": [
        {
          "name": "service unavailable",
          "call": "classify_status_code(503)",
          "expected": "server_error"
        },
        {
          "name": "redirect",
          "call": "classify_status_code(302)",
          "expected": "redirect"
        },
        {
          "name": "unknown",
          "call": "classify_status_code(99)",
          "expected": "unknown"
        }
      ],
      "timeout_seconds": 2,
      "explanation": "The challenge reinforces deterministic branching, status-code categories, and local test execution.",
      "difficulty": 1,
      "tags": [
        "sqlalchemy",
        "session",
        "orm"
      ]
    }
  ],
  "boss_battle": {
    "id": "sqlalchemy-boss",
    "title": "SQLAlchemy Deep Dive boss battle",
    "question_ids": [
      "sqlalchemy-q01",
      "sqlalchemy-q02",
      "sqlalchemy-q03",
      "sqlalchemy-q04",
      "sqlalchemy-q05",
      "sqlalchemy-q06",
      "sqlalchemy-q07",
      "sqlalchemy-q08",
      "sqlalchemy-q09",
      "sqlalchemy-q10",
      "sqlalchemy-q11",
      "sqlalchemy-q12"
    ],
    "passing_threshold": 0.75,
    "reward_xp": 100
  }
}
